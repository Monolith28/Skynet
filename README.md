# Skynet
This script generates a sorted dictionary of unique paths from a specified start vertext to a specified finish vertex.
The network is set up as a linked list, and paths are randomly chosen under a threshold total length which can be estimated by the user.
With a sufficient number of iterations (set here as 1 million) it is highly probable the shortest path is found.
